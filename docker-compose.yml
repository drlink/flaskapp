version: "3"
services:
##### Maria db - Banco de dados###########
  mariadb:
    image: mariadb:10-focal
    container_name: mariadb
    environment:
      - PUID=1000
      - PGID=1000
      - MARIADB_ROOT_PASSWORD=M@$t3RCh3f
      - TZ=America/Sao_Paulo
      - MARIADB_DATABASE=flaskapp
      - MARIADB_USER=flask
      - MARIADB_PASSWORD=iddqdidkfa
    restart: unless-stopped
    ports:
      - 3306:3306
    volumes:
      - ./db:/var/lib/mysql
    networks:
      - app-net
###### Gerenciador do banco de dados ######### endereço: localhost:9006
  adminer:
    image: adminer
    container_name: adminer
    restart: unless-stopped
    ports:
      - 9006:8080
    networks:
      - app-net
###### Network ########
networks:
  app-net:
    driver: bridge
########################################################
###### Usuario inicial banco de dados      #############
# Usuario : flask
# Senha : iddqdidkfa
########################################################
